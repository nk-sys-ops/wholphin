name: Setup
description: "Setup the environment"

runs:
  using: "composite"
  steps:
    # Setup the SDKs
    - name: Set up Python
      uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
      with:
        python-version: '3.14'
    - name: Setup JDK
      uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
      with:
        distribution: zulu
        java-version: '21'
        cache: 'gradle'
    - name: Set versions
      shell: bash
      run: |
        echo "BUILD_TOOLS_VERSION=36.0.0" >> $GITHUB_ENV
        echo "NDK_VERSION=29.0.14206865" >> $GITHUB_ENV
    - name: Setup Android SDK
      uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4
      with:
        # "tools" (the legacy standalone SDK Tools package) was removed from Google's SDK
        # repository years ago and superseded by cmdline-tools, which setup-android already
        # bootstraps on its own -- requesting it here always fails ("Failed to find package
        # 'tools'") regardless of anything else in the workflow.
        packages: "platform-tools build-tools;${{ env.BUILD_TOOLS_VERSION }} ndk;${{ env.NDK_VERSION }}"
    - name: Add NDK to path
      shell: bash
      run: |
        echo "Adding to path: ${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}:${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}/toolchains/llvm/prebuilt/linux-x86_64/bin"
        echo "${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}:${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}/toolchains/llvm/prebuilt/linux-x86_64/bin" >> $GITHUB_PATH
